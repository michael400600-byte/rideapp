const mongoose = require('mongoose');
const rideSchema = new mongoose.Schema({ passenger: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, driver: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, status: { type: String, enum: ['searching','negotiating','accepted','driver_arriving','arrived','in_progress','completed','cancelled'], default: 'searching' }, rideType: { type: String, default: 'economy' }, pricingMode: { type: String, enum: ['suggested', 'negotiate'], required: true }, origin: { address: String, coordinates: [Number] }, destination: { address: String, coordinates: [Number] }, pricing: { suggestedPrice: Number, offeredPrice: Number, finalPrice: Number, counterOffers: [{ driverId: mongoose.Schema.Types.ObjectId, price: Number }] }, paymentMethod: { type: String, default: 'cash' }, distance: { estimated: Number, actual: Number }, duration: { estimated: Number, actual: Number }, rating: { passengerRating: Number, driverRating: Number }, tip: { type: Number, default: 0 } }, { timestamps: true });
module.exports = mongoose.model('Ride', rideSchema);
